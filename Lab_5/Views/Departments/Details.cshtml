@model Lab_5.Department

@{
    ViewData["Title"] = "Детали отделения";
}

<h1>Детали отделения</h1>

<div>
    <h4>Отделение</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.DepartmentName)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.DepartmentName)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.DepartmentPhone)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.DepartmentPhone)
        </dd>
    </dl>
</div>
<div>
    @if (User.Identity.IsAuthenticated && (User.IsInRole("MainAdmin") || User.IsInRole("Admin")))
    {
        <span><a asp-action="Edit" asp-route-id="@Model?.DepartmentId">Редактировать</a> |</span>
    }
    <a asp-action="Index">Назад</a>
</div>
