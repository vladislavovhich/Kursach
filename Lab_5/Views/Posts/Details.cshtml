@model Lab_5.Post

@{
    ViewData["Title"] = "Детали должности";
}

<h1>Детали должности</h1>

<div>
    <h4>Должность</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.PostName)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.PostName)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.PostSalary)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.PostSalary)
        </dd>
    </dl>
</div>
<div>
    @if (User.Identity.IsAuthenticated && (User.IsInRole("MainAdmin") || User.IsInRole("Admin")))
    {
        <span><a asp-action="Edit" asp-route-id="@Model?.PostId">Редактировать</a> |</span>
    }
    <a asp-action="Index">Назад</a>
</div>
