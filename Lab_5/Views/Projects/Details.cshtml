@model Lab_5.Project

@{
    ViewData["Title"] = "Детали проекта";
}

<h1>Детали проекта</h1>

<div>
    <h4>Проект</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.ProjectName)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.ProjectName)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.IsStopped)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.IsStopped)
        </dd>
    </dl>
</div>
<div>
    @if (User.Identity.IsAuthenticated && (User.IsInRole("MainAdmin") || User.IsInRole("Admin")))
    {
        <span><a asp-action="Edit" asp-route-id="@Model?.ProjectId">Редактировать</a> |</span>
    }
    <a asp-action="Index">Назад</a>
</div>
